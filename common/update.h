#ifndef UPDATE_H_
#define UPDATE_H_

/* Confirm the running image, connect to LTE, then wait for Button press
 * to download app0/app1 and reboot.
 * Never returns.
 */
void update_run(void);

#endif /* UPDATE_H_ */
